{
  "name": "staffing-agency-scraper",
  "version": "0.1.0",
  "description": "Database migrations for staffing agency scraper",
  "scripts": {
    "postinstall": "prisma generate"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}

